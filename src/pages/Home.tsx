import React from 'react'
import AgeForm from '../components/AgeForm'

export default function Home() {
  return (
    <main>
      <AgeForm />
      {/* <footer className="mt-8 text-xs text-gray-500">Built with React + TypeScript + Tailwind + React Hook Form</footer> */}
    </main>
  )
}